<?
include "../inc/config.php";
init($content);

//echo getFile($path['style'],$file)."<br>";

/*if (isset($_GET['action']))
{
	switch ($_GET['action'])
	{
		case action1:
		
		$tag_replace_content = "content for this variable";
		//show(html file like a table with tags, array("tag" => $tag_replace_content
		//						"another_tag" => _string_defined_in_language) 				//tag = [tag] in html files
		break;
		case action2:
		break;
		case action3:
		break;
	}
}
else if (isset($_GET['do']))
{
	switch ($_GET['do'])
	{
		case do1:
		//get forms and parameter
		break;
		case do2:
		break;
		case do3:
		break;
	}
}
else 
{
	//content without parms or content with forwarding href
}
*/
?>
